/**
 * A reaction emoji. ``(Remoji)``
 */
export interface Remoji {
  /** The char code of the twemoji. */
  charCode: String,
  /** The number of users who reacted with this emoji. */
  quantity: Number,
}